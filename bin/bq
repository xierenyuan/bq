#!/usr/bin/env node

const program = require('commander')
program
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', 'create template')
  .command('list', 'show list')
  .command('login', '设置用户信息')
  .parse(process.argv)
